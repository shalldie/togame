/**
 * 方向 - 上下左右
 *
 * @export
 * @enum {number}
 */
export enum EDirection {
    /**
     * 上
     */
    UP = 'up',
    /**
     * 下
     */
    DOWN = 'down',
    /**
     * 左
     */
    LEFT = 'left',
    /**
     * 右
     */
    RIGHT = 'right'
}
